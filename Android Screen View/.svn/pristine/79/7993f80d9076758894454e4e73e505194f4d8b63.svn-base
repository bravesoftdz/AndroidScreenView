program AndroidScreenShots;

uses
  FMX.Forms,
  DisplayUnit in 'DisplayUnit.pas' {Form1},
  AndroidScreenGrabber in 'AndroidScreenGrabber.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
